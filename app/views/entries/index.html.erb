<h1><%=@places["name"]%></h1>

<ul>
  <% for entry in @entries %>
    <li>
      <a href="/entries/<%= entry["id"] %>">
      <%= entry["title"] %>
      <%= entry["description"] %>
    </li>
  <% end %>
  <p>
  <a href="/entries/new">New Entry</a>
</p>
</ul>